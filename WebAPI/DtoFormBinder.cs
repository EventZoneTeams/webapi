using Microsoft.AspNetCore.Mvc.ModelBinding;

namespace WebAPI
{
    public class DtoFormBinder
    {
        //public async Task CreateValueProviderAsync(ValueProviderFactoryContext context)
        //{
        //    if (context.ActionContext. == null)
        //    {
        //        throw new ArgumentNullException(nameof(context.ActionContext.ReferenceEquals));
        //    }

        //    var form = context.ActionContext.HttpConte;
        //    var valueProvider = new ListObjectValueProvider(form);
        //    context.ValueProviders.Add(valueProvider);
        //}
    }
}